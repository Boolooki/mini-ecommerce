import LoginForm from "../components/auth/LoginForm";

export default function LoginPage() {
  return (
    <main className="min-h-[50vh] flex items-center justify-center bg-gray-100">
      <LoginForm />
    </main>
  );
}
